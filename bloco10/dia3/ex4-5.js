const upperCaseFunction = (string) => {
  return string.toUpperCase();
}

const returnFirstLetter = (string) => {
  return string[0];
}

const concatStrings = (str1, str2) => {
  return str1 + str2;
}

module.exports = { upperCaseFunction, returnFirstLetter, concatStrings };